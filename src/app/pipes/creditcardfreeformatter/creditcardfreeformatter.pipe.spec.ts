import { CreditcardfreeformatterPipe } from './creditcardfreeformatter.pipe';

describe('CreditcardfreeformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardfreeformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
